import WidgetShell from "./WidgetShell";
import LiveSOSActivity from "../LiveSOSActivity";

const LiveSOSWidget = () => {
  return (
    <WidgetShell title="Live SOS Activity">
      <LiveSOSActivity />
    </WidgetShell>
  );
};

export default LiveSOSWidget;
